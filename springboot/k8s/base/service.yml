apiVersion: v1
kind: Service
metadata:
  name: springboot
  namespace: springboot
  labels:
    app: springboot
spec:
  type: ClusterIP
  selector:
    app: springboot
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
